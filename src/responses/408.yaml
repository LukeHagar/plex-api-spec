description: Request Timeout
content:
  application/json:
    schema:
      type: object
      properties:
        code:
          type: integer
          example: 408
        message:
          type: string
          example: The server timed out waiting for the request.
