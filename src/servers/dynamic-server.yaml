url: "{protocol}://{ip}:{port}"
description: The full address of your Plex Server
variables:
  protocol:
    default: "https"
    enum:
      - "http"
      - "https"
  ip:
    default: "10.10.10.47"
  port:
    default: "32400"
