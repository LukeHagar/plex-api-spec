type: object
required:
  - aspectRatio
description: "Aspect ratio of the video."
properties:
  aspectRatio:
    type: number
    format: float
    description: "Aspect ratio of the video."
    example: 2.35 