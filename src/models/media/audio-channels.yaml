type: object
required:
  - audioChannels
description: "Number of audio channels."
properties:
  audioChannels:
    type: integer
    format: int32
    description: "Number of audio channels."
    example: 6 